== README

This is a small application created around the concepts covered during a phone screen.

Concepts covered are:

* Integration testing

* Acceptance testing

* Unit testing

* DB Relationships, in particular has_many

* HTTP methods(GET, POST)

* JS object instantiation

== Commands

* 'rake' runs the test suite after all initial components are installed.
